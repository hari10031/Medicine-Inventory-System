# API base URL used by the frontend axios client.
# In dev, leave as "/api" so Vite's proxy forwards to the backend (see vite.config.js).
# In production, set this to the full backend URL, e.g. https://api.yourdomain.com/api
VITE_API_BASE_URL=/api
